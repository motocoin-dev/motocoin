Install dependencies using [Homebrew](http://brew.sh):
* `brew install glew glfw3` – OpenGL helper libraries
* `brew install gcc49` – GCC compiler for OpenMP support (clang compiler provided with Xcode doesn't have it).
